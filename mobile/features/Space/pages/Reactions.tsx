import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Reactions = () => {
  return (
    <View style={{ flex: 1, backgroundColor: 'rgb(40, 40,40)', padding: 10 }}>
      <Text style={{ color: 'white' }}>Reactions here</Text>
    </View>
  );
};

export default Reactions;
